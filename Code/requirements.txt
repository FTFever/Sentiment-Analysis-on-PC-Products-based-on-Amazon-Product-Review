# ─────────────────────────────────────────────────────────────
# PC Product Sentiment Analyzer — Python Dependencies
# ─────────────────────────────────────────────────────────────
# Install all dependencies with:
#   pip install -r requirements.txt
#
# Python version: 3.11 recommended
# ─────────────────────────────────────────────────────────────

# ── Data Processing ───────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── Machine Learning ──────────────────────────────────────────
scikit-learn>=1.3.0

# ── Deep Learning (DistilBERT fine-tuning — Step 3) ──────────
torch>=2.0.0
transformers>=4.35.0

# ── Local LLM — Mistral via Ollama (Steps 4, 6b) ─────────────
# Note: Ollama itself must be installed separately from:
# https://ollama.com
# Then run: ollama pull mistral
ollama>=0.1.0

# ── RAG — Vector Database & Embeddings (Steps 6a, 6b) ────────
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ── Document Parsing for RAG knowledge base (Step 6a) ────────
pypdf>=3.0.0
python-docx>=1.0.0

# ── Visualization (Steps 3, 5) ────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Utilities ─────────────────────────────────────────────────
tqdm>=4.65.0
